import React from 'react';

const Hero = () => {
  return (
    <section className="hero">
      <div className="hero-content">
        <h1>Collaborate and build faster, together.</h1>
        <p>Create, share, and get feedback with collaborative boards for rapid development.</p>
        <button className="cta-button">Create Kanban Board</button>
      </div>
    </section>
  );
};

export default Hero;
